# Branching

- `main`: merged playable web preview and stable reviewed changes.
- `agent/flutter-web-preview`: active full-game expansion branch.

Large implementation slices should be reviewed through pull requests before merging into `main`.
