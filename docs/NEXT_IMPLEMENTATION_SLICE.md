# Next Implementation Slice

The next code slice should wire the already implemented domain systems into visible gameplay and persistence:

1. Add a home/navigation shell around the merged Classic game.
2. Add game-mode, advanced-role, mission, and chaos-card configuration screens.
3. Extend role reveal, clue submission, voting, scoring, and result screens for every compatible role and mode.
4. Add browser persistence for profiles, settings, custom packs, statistics, achievements, and active-match recovery.
5. Replace the preview word map with the versioned bundled content repository.
6. Add Firebase service interfaces and emulator-backed private-room flows.

This file is a sequencing note, not a completion claim.
